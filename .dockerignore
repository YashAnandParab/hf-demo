.venv/
_archive/
__pycache__/
*.pyc
.pytest_cache/
.git/
.gitignore

# passed in at runtime via env_file, never baked into the image
.env

# bind-mounted at runtime
data/

# built by its own Dockerfile from ./frontend as the context. Excluded here so a
# 200MB node_modules is never uploaded as part of the backend's build context.
frontend/
